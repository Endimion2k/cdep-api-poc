# Runtime dependencies — minimum needed to run scrapers and generate JSON.
# Versiuni pinuite pentru reproductibilitate; actualizăm via dependabot.

requests==2.32.3
parsel==1.9.1
pydantic==2.9.2
pdfplumber==0.11.4
python-dateutil==2.9.0
truststore==0.10.0       # folosește Windows/macOS cert store (fix pt. antivirus/firewall MITM)
